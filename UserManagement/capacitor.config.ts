import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'user-management',
  webDir: 'dist/user-management',
  bundledWebRuntime: false
};

export default config;
